# Active Log 작업 지침

이 파일은 새 대화나 새 작업 세션에서도 유지해야 하는 프로젝트 공통 규칙이다.

## 작업 시작

1. `PROJECT_CONTEXT.md`와 `TODO.md`를 먼저 읽는다.
2. `git status --short`로 사용자의 기존 변경 사항을 확인하고 보존한다.
3. 실제 코드가 문서와 다르면 코드를 기준으로 판단하되, 차이를 문서에 기록한다.

## 개발 원칙

- 사용자와 결과 설명은 기본적으로 한국어로 작성한다.
- Python 3.11 이상을 기준으로 하며 기존 패키지 구조를 유지한다.
- 설정값과 비밀정보는 환경 변수로 관리한다. `.env`의 실제 값은 문서, 로그, 커밋에 복사하지 않는다.
- 티스토리 게시 동작은 외부 상태를 변경한다. 사용자가 명시적으로 요청하지 않았다면 실제 게시, 로그인, 예약 작업 실행을 하지 않는다.
- 공개 게시 관련 변경은 특히 조심한다. 테스트에서는 실제 브라우저나 티스토리 대신 대역을 사용한다.
- 생성물과 로컬 상태(`data/`, `output/`, 브라우저 프로필, 빌드 폴더)는 소스처럼 취급하지 않는다.
- 관련 변경에는 가능한 범위에서 테스트를 추가하거나 갱신하고 `python -m pytest`로 검증한다.
- 기존 사용자 변경을 되돌리거나 무관한 파일을 정리하지 않는다.

## 프로젝트 기억 갱신

- 구조, 실행법, 안전 정책 또는 중요한 기술 결정이 바뀌면 `PROJECT_CONTEXT.md`를 함께 갱신한다.
- 작업을 시작하거나 마칠 때 `TODO.md`의 현재 작업, 완료 항목, 다음 작업을 최신 상태로 만든다.
- 임시 디버깅 기록이나 긴 대화 내용은 남기지 않고, 다음 작업자가 알아야 할 결정과 근거만 기록한다.
- 문서에 날짜를 적을 때는 `Asia/Seoul` 기준 `YYYY-MM-DD` 형식을 사용한다.

## 기본 검증 명령

```powershell
.\.venv\Scripts\python.exe -m pytest
```

가상환경이 없으면 다음과 같이 준비한다.

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```
