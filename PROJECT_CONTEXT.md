# Active Log 프로젝트 맥락

마지막 갱신: 2026-09-11

## 목적

Active Log 티스토리 블로그용 콘텐츠를 작성·검토하고 게시 대기열로 관리하는 로컬 프로그램이다. 콘텐츠 데이터와 HTML 템플릿을 분리하며, 티스토리 게시에는 로그인된 Chrome 프로필과 Playwright를 사용한다.

## 현재 구성

- 언어/런타임: Python 3.11+
- 웹 관리 화면: FastAPI + Uvicorn (`active_log/web.py`)
- 데스크톱 화면: PySide6 (`active_log/desktop.py`)
- 저장소: SQLite (`active_log/db.py`, 기본 경로 `data/active_log.db`)
- 예약 실행: APScheduler
- 게시 자동화: Playwright (`active_log/publisher.py`)
- 콘텐츠 생성: 검증된 공식 행사 정보와 로컬 템플릿 생성. 서울 축제 수집을 기본으로 하며, 카테고리 선택 시 대한육상연맹(러닝)·GOCAF(캠핑·레저) 공식 일정 보완 목록도 사용한다. GOCAF 회차별 상세 페이지에 공식 포스터가 있으면 해당 이미지를 저장해 게시에 사용한다. 자전거·전국 데이터는 추가 출처 연결 대상이다.
- HTML 생성: Jinja2 (`active_log/templates/post.html.j2`)
- 테스트: pytest (`tests/`)
- 티스토리 스킨: `tistory_skin/`
- 행사 일정 화면: 공개 RSS의 정보 표에서 기간·장소를 읽어 기존·신규 행사 카드를 자동 보충

## 주요 진입점

- `active_log_app.py`: 데스크톱 앱 실행 진입점
- `active-log-desktop.cmd`: Windows 데스크톱 실행 스크립트
- `install-windows.ps1`: Windows 가상환경 및 의존성 설치
- `install-macos.command`: macOS 가상환경 및 의존성 설치
- `active-log-desktop.command`: macOS 데스크톱 실행 스크립트
- `active_log/main.py`: `active-log` CLI
- `active_log/web.py`: 관리 화면, 대기열, 작성 도구, 게시 엔드포인트
- `active_log/service.py`: 초안 생성, 카테고리 순환, 게시 및 예약 실행 조정
- `active_log/db.py`: 게시글과 런타임 상태 관리
- `active_log/quality.py`: 게시 전 품질 검사
- `active_log/official_generator.py`: 서울시 축제 수집, 육상·캠핑 공식 일정 보완, 템플릿 글 생성

## 실행 방법

```powershell
# 데스크톱 앱
.\active-log-desktop.cmd

# 웹 관리 화면
.\.venv\Scripts\active-log.exe serve

# 테스트
.\.venv\Scripts\python.exe -m pytest

# 공식 행사 최대 3개를 검토 대기로 수집
.\.venv\Scripts\active-log.exe collect --count 3

# 공식 행사 최대 3개를 수집하고 실제 공개 게시
.\.venv\Scripts\active-log.exe daily-publish --count 3
```

웹 기본 주소는 `http://127.0.0.1:8000`이며 `/queue`, `/composer`, `/health` 경로를 제공한다.

macOS에서는 저장소 루트의 `install-macos.command`로 설치하고 `active-log-desktop.command`로 실행한다. 운영체제마다 가상환경과 티스토리 로그인 프로필을 별도로 생성한다. 07:20 자동 게시 예약은 현재 Windows 작업 스케줄러에서만 운영한다.

## 데이터와 게시 흐름

1. 데스크톱 상단에서 `자동 · 전체 카테고리` 또는 특정 카테고리를 선택하고 `새 글 작성`을 누르면 검증된 공식 행사 자료로 글을 자동 생성하고 공식 포스터를 내려받는다.
2. SQLite `posts` 테이블에 `draft` 상태로 저장한다.
3. 대기 글은 ID 오름차순(FIFO)으로 선택한다.
4. 게시 직전에 원자적으로 `publishing` 상태를 선점해 중복 게시를 막는다.
5. 성공하면 `private` 또는 `public`, 실패하면 `failed` 상태를 기록한다.
6. 대표 이미지는 `active_log/assets/<topic_key>.<확장자>`, 상세 이미지는 `<topic_key>-detail*` 규칙으로 찾는다.

티스토리 새 글 편집기가 이전 자동저장 본문이나 이미지를 복원할 수 있으므로, 게시 자동화는 새 포스터를 올리기 전에 편집기 본문을 먼저 초기화한다. 초기화를 확인하지 못하면 다른 행사의 이미지가 섞이는 것을 막기 위해 게시를 중단한다.
편집기 자동저장은 제목도 늦게 복원할 수 있으므로, 본문을 초기화한 뒤 제목을 입력하고 발행 버튼을 누르기 직전 실제 제목을 다시 검증한다. 다른 글 제목이 복원됐으면 게시를 중단한다.

새 공식 행사 글은 행사명·연도·일정·시간·장소·요금을 제목과 본문에 명확히 표시하고, 행사명·연도·장소 기반의 구체 태그를 최대 8개 생성한다. 본문은 최소 정보량과 소제목 수를 검사하며 공식 출처가 없는 글은 모든 게시 경로에서 차단한다. `style12` 정보 표는 티스토리 스킨의 첫 행 색상 규칙과 충돌하지 않도록 본문과 스킨 양쪽에서 글자색을 보정한다.

`/?schedule=1` 행사 일정 화면은 `/rss`에 포함된 공개 글 중 `activelog-info-table`이 있는 글의 기간·장소·카테고리를 읽어 기존 고정 카드에 없는 행사를 월별로 자동 추가한다. RSS 제공 범위를 벗어난 오래된 글은 기존 고정 카드 또는 별도 보완이 필요하다.

수동으로 빈 글을 여는 버튼은 제공하지 않는다. `새 글 작성`은 선택한 카테고리의 공식 일정 조회·신규 행사 선택·템플릿 생성·정치 관련 검사·DB 저장·편집 화면 표시를 한 번에 수행한다. 포스터가 공개된 공식 출처만 자동 다운로드한다. 공식 포스터가 없는 행사는 수집 후보에서 제외하며 초안도 저장하지 않는다. 다운로드 실패 시에도 DB에 저장하지 않는다. AI 이미지나 임의의 정보 이미지로 대체하지 않는다. 게시 직전에 공식 보완 목록의 포스터가 로컬에 없으면 다시 다운로드하고, 포스터 파일이 없거나 편집기에 정상 업로드되지 않으면 공개·비공개 게시 모두 중단한다. OpenAI API 키나 사용료는 필요하지 않다. `active_log/official_generator.py`가 공식 출처와 확인일을 본문에 기록하며, 로컬의 모든 행사 식별 키를 수집 제외 대상으로 사용한다.

카테고리 선택 UI는 `러닝`, `자전거`, `캠핑·레저`, `행사·이벤트`를 제공한다. 러닝은 대한육상연맹 연간 일정, 캠핑·레저는 GOCAF 공식 행사 안내를 보완 출처로 사용한다. 자전거 공식 사이트는 접근 제한으로 별도 공개 일정 출처를 추가할 예정이다. 지역은 전국을 허용하되 자동 수집의 목표 비중은 수도권 약 70% / 비수도권 약 30%로 한다.

데스크톱 UI는 티스토리 관리자 화면처럼 왼쪽 고정 메뉴를 사용한다. 대시보드, 글 관리, 티스토리 글 관리, 채널·계정 관리, 설정을 분리한다. 새 글 만들기와 게시 대기함은 `글 관리` 한 화면으로 통합한다. 기본 화면은 글 목록과 실제 게시 모습 미리보기의 두 영역만 사용하고, 제목·카테고리·요약 편집 영역은 `내용 수정`을 눌렀을 때만 연다. 미리보기 상단에는 상태에 따라 게시, 다시 시도 또는 티스토리에서 보기 중 하나만 표시한다. 정렬·삭제·식별 키·태그·출처·본문 HTML 같은 드문 기능은 기본 화면에서 숨기거나 고급 옵션으로 이동한다. `티스토리 글 관리`는 로컬 DB가 아니라 저장된 로그인 세션으로 티스토리 관리자 페이지의 실제 글 제목·상태·카테고리를 읽는다. 게시 시각 내림차순으로 정렬해 가장 최근 글을 1번으로 표시하고 오래된 글은 뒤쪽에 배치한다. 실제 티스토리 글 ID는 수정·삭제 연결용 내부 값으로 유지한다. 수정은 로그인 세션이 적용된 실제 티스토리 편집기를 열며, 실제 삭제는 경고와 `삭제` 문구 입력을 모두 통과한 뒤 실행하고 목록에서 사라졌는지 재확인한다. 채널 관리에는 티스토리 로그인을 제공하며 네이버 블로그와 인스타그램은 게시 기능이 구현되기 전까지 비활성 준비 상태로 표시한다. 계정 비밀번호는 앱 DB에 직접 저장하지 않는다.

관리 화면의 시각 스타일은 따뜻한 밝은 배경, 낮은 채도의 초록색, 둥근 카드와 넓은 여백을 사용한다. 메뉴와 안내 문구는 짧고 자연스러운 표현을 우선해 딱딱한 업무 도구 느낌을 줄인다.

게시 전 미리보기는 실제 공개 티스토리 페이지와 같은 커스텀 헤더, 카테고리 메뉴, 대표 이미지 배경의 글 헤더, 본문 폭과 사이드 프로필 구조를 사용한다. PC 미리보기는 전체 블로그 레이아웃을 축소해 보여주고 모바일 미리보기는 390px 반응형 구조를 사용한다.

개별 글 배너와 메인 최신 일정·최근 글 썸네일은 티스토리 서버 썸네일이 오래된 이미지로 남을 수 있으므로, 별도 고정 배너가 지정되지 않은 글에서는 현재 본문의 첫 번째 이미지를 우선 사용한다.

`글 관리` 목록은 로컬의 대기·실패·게시 중 글과 티스토리에서 동기화한 실제 게시글을 함께 보여준다. 티스토리 동기화에 성공하면 로컬의 게시 완료 기록은 중복 표시하지 않는다. 로컬 글을 선택하면 앱 미리보기와 게시 동작을 사용하고, 티스토리 글을 선택하면 실제 공개 페이지를 미리보기에 열며 티스토리 편집기로 수정한다.

티스토리 글 동기화 시 로그인된 관리자 통계 경로에서 글별 누적 조회수를 최대 6개씩 병렬로 가져온다. 개별 통계 조회 실패는 글 목록 동기화를 막지 않으며, 값을 확인하지 못한 글은 `—`로 표시한다. 조회수는 통합 글 관리, 게시된 글 목록과 홈 요약에 표시한다.

## 중요 안전 조건

- 실제 설정의 `PUBLISH_VISIBILITY`가 게시 방식과 일치해야 한다.
- `publish_private()`은 `private`, `publish_public()`은 `public` 설정에서만 작동한다.
- `AUTO_PUBLISH` 기본 의도는 꺼짐이며, 사용자가 승인하기 전 자동 게시를 켜지 않는다.
- 웹의 상태 변경 요청은 동일 출처 요청만 허용한다.
- `티스토리에 게시`와 `게시 다시 시도`는 명확한 실행 버튼이므로 추가 Y/N 확인 없이 진행한다. 실제 삭제와 저장하지 않은 변경 폐기처럼 복구하기 어려운 작업에는 확인 절차를 유지한다.
- `.env`, SQLite DB, 로그인 브라우저 프로필에는 민감하거나 개인적인 정보가 있을 수 있으므로 내용을 공유하거나 커밋하지 않는다.
- 새로 생성하는 콘텐츠는 정치·정당·선거·정치인·정치 집회 및 논쟁을 다루지 않는다. AI 지침과 `active_log/content_policy.py`의 저장 전 검사로 차단한다.
- `daily-publish`는 실제 공개 게시 명령이다. Windows 예약 작업에서는 사용자 로그인 상태에서만 실행하고 중복 인스턴스를 허용하지 않는다.
- Windows 예약 작업 `Active Log Daily Publish`는 매일 07:20 `daily-publish --count 3`을 실행한다. 자동 수집은 행사·이벤트를 제외하고 `러닝`과 `캠핑·레저`를 교대로 선택한다. 놓친 시각에는 다음 로그인 후 실행하며 로그는 `output/daily-publish.log`에 기록한다.

## 알려진 상태와 확인 사항

- 2026-09-11: 코드에는 조회수 이력 저장용 view_snapshots 테이블, sync-stats CLI와 /stats 화면이 구현되어 있다. 공식 보완 목록에는 테디서킷런·춘천마라톤·빙그레 및 통영 그란폰도가 포함되어 있어, 자전거 출처 및 수집기 연결이 미구현이라는 이전 문서 내용은 현재 코드와 다르다.

- 2026-09-10: 메인 카테고리 영역은 고정 글 대신 `/category/<카테고리>`의 최신 4개 글을 조회하며 첫 글의 현재 본문 이미지를 대표 이미지로 사용한다. 조회 실패 시 카테고리 목록 링크를 보여준다. 전체 글 보기 링크는 `/category`이다. 실제 스킨은 로컬과 일정 파싱 로직이 달라 해당 차이를 보존하고 메인 수정만 적용했다. 실제 편집기는 Monaco(`window.monaco.editor.getModels()`)이며 HTML 편집 진입 시 확인창이 있다.

- 2026-09-10: 사용자 로그인 갱신 후 러닝·자전거·캠핑·레저 카테고리 선택과 실제 게시가 정상 작동했다. 공개 39(테디서킷런)·40(통영 그란폰도)·41(고카프 PART 2) 발행 및 RSS 이미지 확인 완료. 실제 수집 코드의 `DAILY_CATEGORIES`에는 문서의 기존 두 종목과 달리 자전거도 포함되어 있다. 빙그레 그란폰도 로컬 48번은 기존 공개 9번과 동일 대회이므로 새로 게시하지 않는다.

- 2026-09-10: Windows 일일 예약은 07:20:01 시작했으나 0xC000013A로 종료했다. Operational 이력이 비활성이라 종료 유발 원인은 미확정. `daily-publish`는 수집을 모두 마친 뒤 첫 로그를 쓰므로 로그가 없다는 사실만으로 프로세스 미실행을 판단할 수 없다. 9월 9일 로그에는 러닝/캠핑·레저 카테고리 조회 실패 2건이 있다.

- 2026-09-09: 고카프 중복 37번을 사용자 요청으로 삭제하고 35번을 유지했다. 로컬 43번의 공개 URL을 35번으로 연결했다. 수집 시 모든 로컬 topic_key를 제외하고, 게시 전 티스토리 관리자 목록을 페이지별로 읽어 공백·문장부호를 정규화한 동일 제목을 차단한다. 서로 다른 제목의 동일 행사까지 식별하지는 못한다. 목록 조회 실패·반복·100페이지 초과 시 게시를 중단한다.
- 2026-09-09: 공개 36·38번 육상 글은 공식 상세 페이지에 요강(HWP)만 확인되고 해당 행사 포스터를 찾지 못해 기존 내용을 유지했다. 다른 행사 포스터나 생성 이미지로 대체하지 않는다.
- `active_log/config.py`와 `.env.example`의 `publish_visibility` 기본값은 현재 `public`이다.
- `scheduled_cycle()`은 게시 시 `publish_private()`을 호출하므로 `AUTO_PUBLISH=true`이면서 visibility가 `public`이면 실패한다. 자동 게시 정책을 정할 때 두 설정과 구현을 함께 정리해야 한다.
- README는 UTF-8 파일이지만 일부 Windows 콘솔 환경에서 한글이 깨져 출력될 수 있다.
- Git 기록상 현재 기준 커밋은 초기 Active Log 콘텐츠 스튜디오 구성이다. 이후 작업 상태는 `TODO.md`와 `git status`를 함께 확인한다.

## 문서 역할

- `AGENTS.md`: 모든 작업에서 지켜야 할 규칙
- `PROJECT_CONTEXT.md`: 잘 변하지 않는 구조, 정책, 결정
- `TODO.md`: 지금 진행 중인 일과 다음 우선순위
- `README.md`: 사용자를 위한 설치 및 사용법
